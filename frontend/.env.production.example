# Production env for Vercel (or local build with live API)

# Point at your ngrok tunnel while Laravel runs locally:
# VITE_API_URL=https://YOUR-SUBDOMAIN.ngrok-free.app/api

# After ngrok restart, update this value and redeploy Vercel.

VITE_API_URL=https://YOUR-SUBDOMAIN.ngrok-free.app/api
